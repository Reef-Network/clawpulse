# Option A: Connection string (takes precedence)
DATABASE_URL=postgres://reef:reef_local@localhost:5432/clawpulse

# Option B: Individual PG vars (used when DATABASE_URL is not set)
# PGHOST=localhost
# PGPORT=5432
# PGDATABASE=clawpulse
# PGUSER=reef
# PGPASSWORD=reef_local
# PGSSL=false                   # Set "true" for hosted DBs, auto-detected when using DATABASE_URL

PORT=8421                       # Also referenced as CLAWPULSE_PORT by the agent
REEF_DIRECTORY_URL=https://reef-protocol-production.up.railway.app

# REQUIRED — serves double duty:
#   1. Story validation (crawlee + GPT-4o-mini credibility check)
#   2. OpenClaw agent brain (GPT 5.1 via openclaw gateway)
OPENAI_API_KEY=sk-...
